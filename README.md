# بلينكيت كلون — تطبيق تجارة سريعة (React + Vite)

واجهة عربية RTL بأسلوب بلينكيت: هيدر قابل للطي، ثيم مختلف لكل تبويب، سلة، والأسعار بالدينار العراقي.
الصور حالياً إيموجي مؤقتة — استبدلها بصور حقيقية من `src/data/products.js` و`src/data/collections.js`.

## التشغيل محلياً
```bash
npm install
npm run dev        # خادم التطوير على http://localhost:5173
npm run build      # بناء الإنتاج في dist/
npm run lint       # فحص الكود
```

## بنية المشروع — كل جزء منفصل لتطويره دون التأثير على الباقي
```
src/
├── main.jsx                    نقطة الدخول
├── App.jsx                     المنسّق العام: الحالة والتنقل وتركيب الشاشات
├── styles/global.css           كل الأنماط (كلاسات bk-*)
├── constants/colors.js         ألوان الهوية
├── utils/
│   ├── currency.js             الدينار العراقي: CUR / toIQD / fmt
│   └── color.js                مزج الألوان لتأثير التمرير
├── data/                       ← عدّل المحتوى من هنا
│   ├── products.js             المنتجات والأسعار
│   ├── collections.js          بطاقات الأقسام (بقالة، وجبات، جمال…)
│   ├── tabs.jsx                تبويبات الفئات وأيقوناتها
│   └── themes.js               هوية كل تبويب (ألوان الهيدر والبانر والصفوف)
├── hooks/
│   ├── useCart.js              منطق السلة والإجماليات
│   └── useCollapsingHeader.js  طيّ الهيدر عبر متغيّرات CSS (أداء عالٍ)
└── components/                 ← عدّل الواجهة من هنا (كل مكوّن ملف مستقل)
    SplashScreen، DeliveryInfo، SearchBar، CategoryTabs،
    WelcomeHero، Hero، Bestsellers، TileGrid، ProductRow، ProductCard،
    HomeContent، ThemedContent، Listing، CartBar، BottomNav
```

## الرفع على GitHub
```bash
git init
git add .
git commit -m "الإصدار الأول: واجهة بلينكيت"
git branch -M main
git remote add origin https://github.com/USERNAME/blinkit-clone.git
git push -u origin main
```

## النشر على Netlify
1. من لوحة Netlify: **Add new site ← Import an existing project ← GitHub** واختر المستودع.
2. الإعدادات تُقرأ تلقائياً من `netlify.toml` (أمر البناء `npm run build` ومجلد النشر `dist`).
3. اضغط **Deploy** — وكل `git push` لاحق ينشر تلقائياً.


## البوابات وبيانات الدخول (تجريبية)

| البوابة | الرابط | الدخول |
|---|---|---|
| الأدمن | `/admin` | الرمز من الإعدادات (افتراضياً **1234**) |
| التاجر | `/merchant` | النخيل **1111** · بيوتي لاند **2222** · تك ستور **3333** |
| المندوب | `/courier` | أحمد **1111** · حسن **2222** · مرتضى **3333** (موقوف) |

### الجديد في تطوير اللوحات
- **المالية (أدمن):** صافي الربح = عمولات + رسوم − أجور المندوبين، تسويات التجار (ادفع → يؤكد التاجر) ونقد المندوبين (يسلّم → تؤكد أنت).
- **بوابة الجاهزية:** الطلب متعدد المتاجر لا يصبح «جاهز للتوصيل» إلا بعد أن يؤشر كل تاجر «منتجاتي جاهزة ✅».
- **حالة جديدة:** «وصل المندوب» بين الطريق والتسليم — تظهر في تتبع الزبون.
- **حسابات مستقلة للمندوبين** بكلمة مرور يديرها الأدمن (مع إيقاف الحساب).
- **أزرار تواصل** في طلبات الأدمن: اتصال / واتساب / الموقع على الخريطة.
- **مزامنة حية بين التبويبات** + **تنبيه صوتي** للطلب الجديد + **وضع ليلي** لكل بوابة.
- إعدادات جديدة: أجرة المندوب (أساسية + لكل متجر إضافي)، رمز الأدمن، رقم واتساب.

## صفحات المتجر (من فيديوهات التطبيق الأصلي)

> **شريط السلة** يعرض صوراً دائرية مصغّرة لأحدث المنتجات المُضافة (كما في بلينكيت)، و**الشريط السفلي** يُخفى تلقائياً داخل الصفحات الكاملة (السلة/الدفع/التتبع) كي لا يحجب أزرارها.

> **شريط السلة** يعرض صوراً دائرية مصغّرة لأحدث المنتجات المُضافة (كما في بلينكيت)، و**الشريط السفلي** يُخفى تلقائياً داخل الصفحات الكاملة (السلة/الدفع/التتبع) كي لا يحجب أزرارها.
تفاصيل المنتج (مواصفات، وصف، سياسة استبدال، مشابهة، اشترى الناس أيضاً) · السلة الكاملة (شحنات متعددة، فاتورة، توصيل مجاني فوق حد، تعليمات التوصيل، بقشيش) · الدفع (محافظ عراقية + نقداً) · اختيار وإضافة العناوين · **تتبّع الطلب الحي** (خريطة + خط حالة يتحدّث مع إجراءات التاجر والمندوب) · طلباتي + إعادة الطلب · البحث الحي · صفحة التصنيف بشريط جانبي وفرز.

> **شريط السلة** يعرض الآن صوراً دائرية مصغّرة لأحدث المنتجات المُضافة (كما في بلينكيت)، والشريط السفلي يُخفى تلقائياً داخل الصفحات الكاملة كي لا يحجب أزرارها.

### دورة الطلب كاملة (جرّبها!)
1. من المتجر `/` أضف منتجات ← «عرض السلة» ← **تأكيد الطلب**.
2. يظهر الطلب فوراً عند **الأدمن** وعند **التاجر** بحالة «جديد».
3. التاجر: قبول ← جاهز للتوصيل. 4. المندوب: استلام ← **تم التوصيل**. 5. أرباحه = أجر التوصيلة **+ بقشيش الزبون**. 6. الزبون يتابع كل ذلك **لحظياً** من شاشة التتبع، وله إلغاء الطلب وهو «جديد».

> **هل أحتاج Supabase/SQL الآن؟ لا.** الموقع يعمل كاملاً بتخزين محلي (`localStorage`) دون أي إعداد. عند رغبتك بقاعدة بيانات حقيقية (مشاركة البيانات بين الأجهزة)، سيُضاف ملف SQL جاهز للصق في Supabase وربطه عبر تعديل `src/store/appStore.js` فقط — بقية الموقع لن تتغير.

## أفكار للتطوير التالي
- صفحة تفاصيل المنتج وتتبّع الطلب للزبون.
- استبدال الإيموجي بصور: أضف حقل `img` في `products.js` واعرضه داخل `ProductCard.jsx`.
- ربط قاعدة بيانات حقيقية (Supabase/Firebase) بدل `localStorage` عبر تعديل `src/store/appStore.js` فقط.
